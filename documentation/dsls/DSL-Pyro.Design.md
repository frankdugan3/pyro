<!--
This file was generated by Spark. Do not edit it by hand.
-->
# Pyro.Design



## config
Pyro-specific configuration that complements the DTCG token tree.


### Nested DSLs
 * [base_layer](#config-base_layer)
   * selector
     * property
   * raw





### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`namespace`](#config-namespace){: #config-namespace .spark-required} | `String.t` |  | Prefix for generated CSS custom property names (e.g., `"pyro"` → `--pyro-*`). |
| [`tailwind_preamble`](#config-tailwind_preamble){: #config-tailwind_preamble } | `String.t` |  | Raw CSS for Tailwind custom variants. Included before the auto-generated `@theme` block when `support_tailwind?: true` on the design module. |


### config.base_layer
CSS rules for `@layer base`. Rendered when `manage_base_layer?: true`.


### Nested DSLs
 * [selector](#config-base_layer-selector)
   * property
 * [raw](#config-base_layer-raw)





### config.base_layer.selector
```elixir
selector selector
```


A CSS rule — one or more selectors and a declaration block — used
inside the `config.base_layer` section to author rules emitted
into `@layer base` of the generated CSS.

config do
base_layer do
selector ["html", "body"] do
property "margin", "0"
end
end
end

Pass a single string for one selector or a list of strings to share
a declaration block across selectors. Property values may reference
design tokens via `$token.path`, which the writer resolves before
emission.


### Nested DSLs
 * [property](#config-base_layer-selector-property)




### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`selector`](#config-base_layer-selector-selector){: #config-base_layer-selector-selector .spark-required} | `String.t \| list(String.t)` |  | One or more CSS selectors that share this declaration block. |



### config.base_layer.selector.property
```elixir
property name, value
```


A single CSS declaration inside a `selector` block.

selector "html, body" do
property "margin", "0"
property "color", "$color.brand.500"
end

`name` is the CSS property name. `value` is the CSS value;
`$token.path` references are resolved against the design's token
tree before emission.






### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`name`](#config-base_layer-selector-property-name){: #config-base_layer-selector-property-name .spark-required} | `String.t` |  | CSS property name. |
| [`value`](#config-base_layer-selector-property-value){: #config-base_layer-selector-property-value .spark-required} | `String.t` |  | CSS value. `$token.path` references resolve against the design's token tree. |











### config.base_layer.raw
```elixir
raw css
```


Raw CSS string emitted into `@layer base` verbatim, after
`$token.path` reference resolution. Use only for rules that the
structured `selector` entity cannot express — primarily at-rules
whose body itself contains rules (`@keyframes`, `@supports`,
conditional `@media` blocks). Prefer `selector` for everything
else; `raw` skips per-property validation.

config do
base_layer do
raw """
@keyframes flash {
0% { background: $color.brand.500/50; }
100% { background: transparent; }
}
"""
end
end






### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`css`](#config-base_layer-raw-css){: #config-base_layer-raw-css .spark-required} | `String.t` |  | Raw CSS body. `$token.path` references resolve via the design's namespace. |












## design
A DTCG 2025.10 token tree.

Authors use macros from `Pyro.Design.Macros` (`color`, `dimension`,
`shadow`, `typography`, `ref`, `root`, …) — values are parsed
eagerly at compile time. See `Pyro.Design.Macros` for the full
catalog with executable examples. Groups nest recursively.
Modifiers with their context children model DTCG resolver
modifiers.


### Nested DSLs
 * [group](#design-group)
   * token
 * [modifier](#design-modifier)
   * context
     * group
       * token
     * token
 * [token](#design-token)





### design.group
```elixir
group name
```


A DTCG group — a container of tokens and/or nested groups.

Groups carry optional `$type` (inherited by descendants),
`$description`, `$deprecated`, `$extensions`, and `$extends`.
Inside a group block, the typed authoring macros from
`Pyro.Design.Macros` (`color`, `dimension`, `shadow`, …) are
available.


### Nested DSLs
 * [token](#design-group-token)




### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`name`](#design-group-name){: #design-group-name .spark-required} | `atom` |  | Group name. Becomes the DTCG object key at this level. |
### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`type`](#design-group-type){: #design-group-type } | `:color \| :dimension \| :font_family \| :font_weight \| :duration \| :cubic_bezier \| :number \| :stroke_style \| :border \| :transition \| :shadow \| :gradient \| :typography` |  | Default DTCG `$type` for descendants that don't set one explicitly. |
| [`description`](#design-group-description){: #design-group-description } | `String.t` |  | DTCG `$description`. |
| [`deprecated`](#design-group-deprecated){: #design-group-deprecated } | `boolean \| String.t` | `false` | DTCG `$deprecated`. `true`, `false`, or an explanation string. |
| [`extends`](#design-group-extends){: #design-group-extends } | `any` |  | DTCG `$extends` reference (a `%Pyro.Design.Reference{}` or string that parses into one). Validated at compile time; inheritance is a resolver concern. |
| [`extensions`](#design-group-extensions){: #design-group-extensions } | `%{optional(String.t) => any}` | `%{}` | DTCG `$extensions` map keyed by reverse-domain vendor ids. |


### design.group.token
```elixir
token name
```


Generic DTCG token escape hatch.

Prefer the typed authoring macros in `Pyro.Design.Macros` (`color`,
`dimension`, `shadow`, `typography`, `ref`, `root`, …) when writing
a design. They parse and validate inputs at compile time. This generic `token` entity is for programmatically-built
tokens, pre-parsed values, and `$ref`-only tokens that bypass
per-type schemas.

Every DTCG token in a compiled design is a `%Pyro.Design.Token{}`,
regardless of whether it was authored via a typed macro or the
generic entity.




### Examples
```
token :primary_action, ref: "#/color/brand/500"
```

```
token :custom, type: :color, value: %Color.Oklch{l: 0.7, c: 0.2, h: 250}
```



### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`name`](#design-group-token-name){: #design-group-token-name .spark-required} | `atom` |  | Token name. Becomes the DTCG object key under its group. |
### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`type`](#design-group-token-type){: #design-group-token-type } | `:color \| :dimension \| :font_family \| :font_weight \| :duration \| :cubic_bezier \| :number \| :stroke_style \| :border \| :transition \| :shadow \| :gradient \| :typography` |  | DTCG `$type`. Inherited from nearest ancestor group if omitted. |
| [`value`](#design-group-token-value){: #design-group-token-value } | `any` |  | Parsed `$value`. Mutually exclusive with `:ref`. |
| [`ref`](#design-group-token-ref){: #design-group-token-ref } | `any` |  | `$ref` pointer (string or `%Pyro.Design.Reference{}`). Mutually exclusive with `:value`. |
| [`description`](#design-group-token-description){: #design-group-token-description } | `String.t` |  | DTCG `$description`. |
| [`deprecated`](#design-group-token-deprecated){: #design-group-token-deprecated } | `boolean \| String.t` | `false` | DTCG `$deprecated`. `true`, `false`, or an explanation string. |
| [`extensions`](#design-group-token-extensions){: #design-group-token-extensions } | `%{optional(String.t) => any}` | `%{}` | DTCG `$extensions` map keyed by reverse-domain vendor ids. |
| [`root?`](#design-group-token-root?){: #design-group-token-root? } | `boolean` | `false` | True when this token is the DTCG `$root` child of its group. |










### design.modifier
```elixir
modifier name
```


A DTCG resolver-module modifier — a named conditional layer with two
or more **contexts**, each supplying token overrides for a specific
condition (e.g. `light` / `dark` color modes, `compact` / `comfortable`
density).

modifier :color_mode do
description "Light vs dark."
default :light

context :light do
color :bg, "#fff"
end

context :dark do
color :bg, "#000"
end
end

Optionally names a `default` context to apply when the resolver input
omits this modifier key. The Design compiler persists these under
`:design_modifiers`.


### Nested DSLs
 * [context](#design-modifier-context)
   * group
     * token
   * token




### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`name`](#design-modifier-name){: #design-modifier-name .spark-required} | `atom` |  | Modifier name (e.g. `:color_mode`, `:density`). |
### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`description`](#design-modifier-description){: #design-modifier-description } | `String.t` |  | DTCG `$description`. |
| [`default`](#design-modifier-default){: #design-modifier-default } | `atom` |  | Name of the context to apply when the resolver input omits this modifier key. |
| [`extensions`](#design-modifier-extensions){: #design-modifier-extensions } | `%{optional(String.t) => any}` | `%{}` | DTCG `$extensions` — vendor-namespaced metadata. |


### design.modifier.context
```elixir
context name
```


A DTCG resolver-module context — a named slot inside a modifier.

Each context holds the token overrides that apply when this context
is selected at resolver runtime. Children are the usual Design
vocabulary — tokens, groups, and nested modifiers.

modifier :color_mode do
default :light

context :light do
color :bg, "#fff"
end

context :dark do
color :bg, "#000"
end
end


### Nested DSLs
 * [group](#design-modifier-context-group)
   * token
 * [token](#design-modifier-context-token)




### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`name`](#design-modifier-context-name){: #design-modifier-context-name .spark-required} | `atom` |  | Context name (e.g. `:light`, `:dark`). |
### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`description`](#design-modifier-context-description){: #design-modifier-context-description } | `String.t` |  | DTCG `$description`. |
| [`extensions`](#design-modifier-context-extensions){: #design-modifier-context-extensions } | `%{optional(String.t) => any}` | `%{}` | DTCG `$extensions` — vendor-namespaced metadata. |


### design.modifier.context.group
```elixir
group name
```


A DTCG group — a container of tokens and/or nested groups.

Groups carry optional `$type` (inherited by descendants),
`$description`, `$deprecated`, `$extensions`, and `$extends`.
Inside a group block, the typed authoring macros from
`Pyro.Design.Macros` (`color`, `dimension`, `shadow`, …) are
available.


### Nested DSLs
 * [token](#design-modifier-context-group-token)




### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`name`](#design-modifier-context-group-name){: #design-modifier-context-group-name .spark-required} | `atom` |  | Group name. Becomes the DTCG object key at this level. |
### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`type`](#design-modifier-context-group-type){: #design-modifier-context-group-type } | `:color \| :dimension \| :font_family \| :font_weight \| :duration \| :cubic_bezier \| :number \| :stroke_style \| :border \| :transition \| :shadow \| :gradient \| :typography` |  | Default DTCG `$type` for descendants that don't set one explicitly. |
| [`description`](#design-modifier-context-group-description){: #design-modifier-context-group-description } | `String.t` |  | DTCG `$description`. |
| [`deprecated`](#design-modifier-context-group-deprecated){: #design-modifier-context-group-deprecated } | `boolean \| String.t` | `false` | DTCG `$deprecated`. `true`, `false`, or an explanation string. |
| [`extends`](#design-modifier-context-group-extends){: #design-modifier-context-group-extends } | `any` |  | DTCG `$extends` reference (a `%Pyro.Design.Reference{}` or string that parses into one). Validated at compile time; inheritance is a resolver concern. |
| [`extensions`](#design-modifier-context-group-extensions){: #design-modifier-context-group-extensions } | `%{optional(String.t) => any}` | `%{}` | DTCG `$extensions` map keyed by reverse-domain vendor ids. |


### design.modifier.context.group.token
```elixir
token name
```


Generic DTCG token escape hatch.

Prefer the typed authoring macros in `Pyro.Design.Macros` (`color`,
`dimension`, `shadow`, `typography`, `ref`, `root`, …) when writing
a design. They parse and validate inputs at compile time. This generic `token` entity is for programmatically-built
tokens, pre-parsed values, and `$ref`-only tokens that bypass
per-type schemas.

Every DTCG token in a compiled design is a `%Pyro.Design.Token{}`,
regardless of whether it was authored via a typed macro or the
generic entity.




### Examples
```
token :primary_action, ref: "#/color/brand/500"
```

```
token :custom, type: :color, value: %Color.Oklch{l: 0.7, c: 0.2, h: 250}
```



### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`name`](#design-modifier-context-group-token-name){: #design-modifier-context-group-token-name .spark-required} | `atom` |  | Token name. Becomes the DTCG object key under its group. |
### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`type`](#design-modifier-context-group-token-type){: #design-modifier-context-group-token-type } | `:color \| :dimension \| :font_family \| :font_weight \| :duration \| :cubic_bezier \| :number \| :stroke_style \| :border \| :transition \| :shadow \| :gradient \| :typography` |  | DTCG `$type`. Inherited from nearest ancestor group if omitted. |
| [`value`](#design-modifier-context-group-token-value){: #design-modifier-context-group-token-value } | `any` |  | Parsed `$value`. Mutually exclusive with `:ref`. |
| [`ref`](#design-modifier-context-group-token-ref){: #design-modifier-context-group-token-ref } | `any` |  | `$ref` pointer (string or `%Pyro.Design.Reference{}`). Mutually exclusive with `:value`. |
| [`description`](#design-modifier-context-group-token-description){: #design-modifier-context-group-token-description } | `String.t` |  | DTCG `$description`. |
| [`deprecated`](#design-modifier-context-group-token-deprecated){: #design-modifier-context-group-token-deprecated } | `boolean \| String.t` | `false` | DTCG `$deprecated`. `true`, `false`, or an explanation string. |
| [`extensions`](#design-modifier-context-group-token-extensions){: #design-modifier-context-group-token-extensions } | `%{optional(String.t) => any}` | `%{}` | DTCG `$extensions` map keyed by reverse-domain vendor ids. |
| [`root?`](#design-modifier-context-group-token-root?){: #design-modifier-context-group-token-root? } | `boolean` | `false` | True when this token is the DTCG `$root` child of its group. |










### design.modifier.context.token
```elixir
token name
```


Generic DTCG token escape hatch.

Prefer the typed authoring macros in `Pyro.Design.Macros` (`color`,
`dimension`, `shadow`, `typography`, `ref`, `root`, …) when writing
a design. They parse and validate inputs at compile time. This generic `token` entity is for programmatically-built
tokens, pre-parsed values, and `$ref`-only tokens that bypass
per-type schemas.

Every DTCG token in a compiled design is a `%Pyro.Design.Token{}`,
regardless of whether it was authored via a typed macro or the
generic entity.




### Examples
```
token :primary_action, ref: "#/color/brand/500"
```

```
token :custom, type: :color, value: %Color.Oklch{l: 0.7, c: 0.2, h: 250}
```



### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`name`](#design-modifier-context-token-name){: #design-modifier-context-token-name .spark-required} | `atom` |  | Token name. Becomes the DTCG object key under its group. |
### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`type`](#design-modifier-context-token-type){: #design-modifier-context-token-type } | `:color \| :dimension \| :font_family \| :font_weight \| :duration \| :cubic_bezier \| :number \| :stroke_style \| :border \| :transition \| :shadow \| :gradient \| :typography` |  | DTCG `$type`. Inherited from nearest ancestor group if omitted. |
| [`value`](#design-modifier-context-token-value){: #design-modifier-context-token-value } | `any` |  | Parsed `$value`. Mutually exclusive with `:ref`. |
| [`ref`](#design-modifier-context-token-ref){: #design-modifier-context-token-ref } | `any` |  | `$ref` pointer (string or `%Pyro.Design.Reference{}`). Mutually exclusive with `:value`. |
| [`description`](#design-modifier-context-token-description){: #design-modifier-context-token-description } | `String.t` |  | DTCG `$description`. |
| [`deprecated`](#design-modifier-context-token-deprecated){: #design-modifier-context-token-deprecated } | `boolean \| String.t` | `false` | DTCG `$deprecated`. `true`, `false`, or an explanation string. |
| [`extensions`](#design-modifier-context-token-extensions){: #design-modifier-context-token-extensions } | `%{optional(String.t) => any}` | `%{}` | DTCG `$extensions` map keyed by reverse-domain vendor ids. |
| [`root?`](#design-modifier-context-token-root?){: #design-modifier-context-token-root? } | `boolean` | `false` | True when this token is the DTCG `$root` child of its group. |














### design.token
```elixir
token name
```


Generic DTCG token escape hatch.

Prefer the typed authoring macros in `Pyro.Design.Macros` (`color`,
`dimension`, `shadow`, `typography`, `ref`, `root`, …) when writing
a design. They parse and validate inputs at compile time. This generic `token` entity is for programmatically-built
tokens, pre-parsed values, and `$ref`-only tokens that bypass
per-type schemas.

Every DTCG token in a compiled design is a `%Pyro.Design.Token{}`,
regardless of whether it was authored via a typed macro or the
generic entity.




### Examples
```
token :primary_action, ref: "#/color/brand/500"
```

```
token :custom, type: :color, value: %Color.Oklch{l: 0.7, c: 0.2, h: 250}
```



### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`name`](#design-token-name){: #design-token-name .spark-required} | `atom` |  | Token name. Becomes the DTCG object key under its group. |
### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`type`](#design-token-type){: #design-token-type } | `:color \| :dimension \| :font_family \| :font_weight \| :duration \| :cubic_bezier \| :number \| :stroke_style \| :border \| :transition \| :shadow \| :gradient \| :typography` |  | DTCG `$type`. Inherited from nearest ancestor group if omitted. |
| [`value`](#design-token-value){: #design-token-value } | `any` |  | Parsed `$value`. Mutually exclusive with `:ref`. |
| [`ref`](#design-token-ref){: #design-token-ref } | `any` |  | `$ref` pointer (string or `%Pyro.Design.Reference{}`). Mutually exclusive with `:value`. |
| [`description`](#design-token-description){: #design-token-description } | `String.t` |  | DTCG `$description`. |
| [`deprecated`](#design-token-deprecated){: #design-token-deprecated } | `boolean \| String.t` | `false` | DTCG `$deprecated`. `true`, `false`, or an explanation string. |
| [`extensions`](#design-token-extensions){: #design-token-extensions } | `%{optional(String.t) => any}` | `%{}` | DTCG `$extensions` map keyed by reverse-domain vendor ids. |
| [`root?`](#design-token-root?){: #design-token-root? } | `boolean` | `false` | True when this token is the DTCG `$root` child of its group. |









## icons
Named icons (Pyro-specific; non-DTCG).

### Nested DSLs
 * [icon](#icons-icon)





### icons.icon
```elixir
icon name, svg
```


Declare a named icon with inline SVG.





### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`name`](#icons-icon-name){: #icons-icon-name .spark-required} | `atom` |  | Icon name (e.g., `:x`, `:chevron_down`). |
| [`svg`](#icons-icon-svg){: #icons-icon-svg .spark-required} | `String.t` |  | Inline SVG markup. Use the `~SVG` sigil for syntax highlighting. |











<style type="text/css">.spark-required::after { content: "*"; color: red !important; }</style>
