defmodule Pyro.Component.CoreTest do
  @moduledoc false

  # IO.inspect(Pyro.TestComponents.Core.__info__(:attributes), structs: false)
  # IO.inspect(Pyro.TestComponents.Core.__info__(:functions), structs: false)
  # IO.inspect(Pyro.TestComponents.Core.__info__(:macros), structs: false)
  # IO.inspect(Pyro.TestComponents.Core.__info__(:struct), structs: false)
  # Pyro.TestComponents.Custom.spark_dsl_config()
  # |> Map.get([:components])
  # |> IO.inspect()
  #
  # dbg(Pyro.TestComponents.Custom.__info__(:functions))
  # dbg(Pyro.TestComponents.Custom.something(%{}))
end
