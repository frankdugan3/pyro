defmodule Mix.Tasks.Pyro.Gen do
  @moduledoc false
  use Mix.Task

  def run(_arv) do
    # :pyro
    # |> Application.get_env(:extensions, [])
    # |> Enum.each(&Mix.Tasks.Pyro.Generators.Components.generate/1)
    # |> Enum.each(&Mix.Tasks.Pyro.Generators.Css.generate/1)
  end
end
